export default function RedaccioModule({ folder }) {
  return (
    <div>
      <h1>{folder.name}</h1>
      <p>Mode: Redacció de documents policials</p>
      <p>Aquí la IA generarà documents basant-se en actes d'exemple.</p>
    </div>
  );
}
